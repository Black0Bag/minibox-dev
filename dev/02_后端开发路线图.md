# minibox 后端维护路线图

## 当前结论

后端核心阶段完成，已通过全仓 `go test -count=1 ./...`、`go vet ./...`、构建和隔离实例冒烟。后端不再以“继续补齐所有规划功能”为主，而以**稳定契约、安全加固和前端阻塞修复**为主。

## 已完成能力

- 多供应商 OpenAI 兼容 LLM、功能级模型路由和供应商降级。
- Embedding query/passage、维度校验和语义/全文混合检索。
- SQLite 迁移、FTS5 中文预分词、向量、编译、蒸馏和快照。
- Agent 状态机、MemoryGate、Plan-first、工具权限和运行持久化。
- REST、SSE 断线回放、WebSocket JSON-RPC 单读循环和乱序回执。
- 设备 Hub、审计、护栏和 18 个设备工具。
- 调度、团队协作、监控、备份、升级机制和首次向导。

## 前端开发前的后端待办

| 优先级 | 事项 | 状态 |
|---|---|---|
| ~~P0~~ | 决定并实现非 loopback REST 认证 | ✅ 2026-09-02 完成：固定 Bearer Token（`data/auth.token`，0600），白名单仅 health/ready/device-ws；401 符合 RFC 9110/6750/7807；常量时间比较 |
| ~~P0~~ | 审批 API 契约 | ✅ 已实现 `POST /api/v1/approvals/{run_id}`，Agent 暂停等外部提交，不再自动拒绝 |
| ~~P0~~ | 修复/冻结团队 `TaskDependency` JSON tag | ✅ 2026-09-02 完成：`id` / `depends_on`，且 teamwork 全域 30 个字段统一 snake_case + 契约测试锁定 |
| ~~P1~~ | Linux x86_64 race CI | ✅ job 已通过（据其日志修复 5 处 data race） |
| P1 | OpenAPI 契约 | 未做；契约已冻结，可择时生成 |
| P1 | 明确重启持久化范围 | 部分完成：会话消息已落 `conversation_log`；团队项目、SSE 历史、编译作业仍在内存 |
| P2 | 分拆大型 app handler 文件 | 未做（`http_handlers.go` 1030 行） |
| P2 | 浏览器代理链路 | 未做：仅有 WS 方法常量与占位 handler，Agent 无对应工具、Hub 无下发路径 |

## 维护门禁

```bash
gofmt -l internal cmd
go vet ./...
go test -count=1 ./...
go build ./cmd/minibox/
golangci-lint run          # v2.12.2，与 CI 同版本
```

- `-race` 本机跑不了（Android/arm64 无 ThreadSanitizer），依赖 CI 的
  Linux x86_64 job；改动并发代码后必须看该 job 结果，不能只看本机绿。
- LLM/Embedding 改动先做 HTTP fixture，再做真实供应商探针。
- 数据库改动先在新测试库跑迁移、回填和 CRUD。
- WS 改动必须保留单读循环并跑并发乱序响应测试；通知回调结果用 channel 交接，
  不要用共享变量 + Sleep（会构成 data race）。
- SSE 改动必须跑 HTTP `Last-Event-ID` 重连测试。
- 改动 REST 响应结构必须同步 `docs/api.md` 与前端 DTO 文档。

## 不在普通维护中执行

- 真实 Android 点击、输入、通知、剪贴板等副作用。
- 自升级真实替换稳定二进制。
- 主数据库恢复或覆盖。
- 外部平台写入和正式发布。

这些操作都需要独立授权、备份和回滚计划。