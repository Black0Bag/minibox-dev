# minibox 后端最终验收摘要

## 最终状态

2026-08-25，后端核心修复、回归测试、构建和隔离冒烟已完成，可以进入 Android 前端工程准备。该结论取代 2026-08-24 的中间阻塞结论。

## 已完成修复

- 兼容 OpenAI 供应商在非流式请求中返回 SSE。
- 修复 Embedding URL/字段/query-passage/维度契约。
- 修复 FTS5 中文预分词索引和历史回填。
- WebSocket 改为单读循环 + pending map，支持并发乱序回执。
- SSE `Last-Event-ID` 原子订阅与历史回放。
- 知识编译接入 LLM 结构化提炼并保留失败降级。
- 18 个设备工具接入 Agent 工具系统并标注风险/审批。
- 修复团队分诊、JSON tag、调度/设备 API 等已发现问题。

## 质量证据

- `go test -count=1 ./...` 通过（34 包）。
- `go vet ./...` 通过。
- `gofmt -l internal cmd` 无输出。
- `golangci-lint run`（v2.12.2，与 CI 同版本）0 issues。
- `go test -race ./...` 通过（GitHub Actions Linux x86_64 job）。
- 最新源码构建成功。
- REST 健康/就绪/配置/会话/知识/工具/权限/设备/调度/监控完成冒烟。
- SSE 握手与 HTTP 重连回放测试通过。
- 模拟设备 WS 命令往返、并发乱序响应和工具审批测试通过。
- CI 四个 job（测试 / 竞态检测 / 静态检查 / 依赖审计）2026-09-02 首次全绿。

## 2026-09-02 契约与并发加固

- 错误响应全局统一为 RFC 9457 Problem Details（401/404/405 与业务错误一致），
  401 补齐 `WWW-Authenticate` 挑战头，Token 比较改常量时间。
- 认证中间件收敛为唯一实现（原有两份并存，未接线的那份缺 `/device/ws` 白名单）。
- teamwork 域 30 个字段补齐 JSON tag，统一 lower_snake_case，10 个契约测试锁定。
- 修复 5 处 data race（HTTP Server 字段、WS 通知回调、监控历史缓冲、会话状态）。
- 深挖出 3 个真实缺陷：`driveRun` 状态机实际未推进、`Send` 会话 ID 错乱、
  `finishRun` 未清理审批入口。
- golangci-lint 36 项告警清零。

详情见后端仓库 `docs/validation.md`。

## 外部服务说明

真实 LLM 连续测试出现部分供应商 429 RPM 限流。已成功取得有效回答，失败日志明确为上游配额限制；这不等价于代码错误，也不能作为生产 SLA。

## 未覆盖边界

- 本机 Android/arm64 不能运行 ThreadSanitizer，`-race` 证据由 CI 提供（已通过）。
- 未执行真实 Android 设备副作用。
- 未执行真实自升级替换、主数据库恢复和外部平台写操作。
- 浏览器代理未实装：仅有 WS 方法常量与占位 handler。

## 前端前置缺口

已解决（2026-09-02）：

- REST 认证：固定 Bearer Token（`data/auth.token`），非 loopback 可用。
- 审批提交：`POST /api/v1/approvals/{run_id}` 已公开，可做真实允许/拒绝交互。
- DTO 命名：teamwork 全域统一 snake_case 并有测试锁定。

仍开放：

- OpenAPI 未生成（契约已冻结，可择时进行）。
- 团队项目、SSE 历史、编译作业仍在内存，重启即失。
- 浏览器代理链路待实装。

详细代码事实以后端仓库 `docs/validation.md`、`docs/api.md`、`docs/sse.md`、`docs/websocket.md` 为准。