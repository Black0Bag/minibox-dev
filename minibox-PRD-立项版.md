# minibox 产品需求文档（PRD）· 立项版

> 版本：v1.0 立项版
> 日期：2026-08-09
> 状态：立项决策定稿，已写入进度跟踪文档，第 1 步完成
> 依据：子步一需求分析 + 子步二可行性评估
> 冲突裁决：本 PRD 与 dev/ 既有文档冲突时，以本 PRD 为准（重新立项基准）

---

## 1. 项目定位（一句话）

> **一个 Go 后端为「中枢大脑」、以安卓 APP / Win 程序 / 各桌面 UI 为「眼耳口手」的私有化强 Agent 系统。单文件二进制下载即运行，对标市面所有 agent（优点全要、缺点全避），主供作者自用，开源是附带。**

## 2. 解决什么问题

- 现有 agent 部署复杂（依赖一堆文件）→ 要单文件二进制下载即运行
- agent 记忆散装文本难迁移 → 要知识库作唯一记忆系统
- agent 前后端耦合或单端 → 要前后端强分离
- 单机单端能力受限 → 要多前端互联
- 数据隐私 → 私有化
- 长期使用怕推倒重来 → 接受推倒重来，但尽量稳
- 能力看齐或超过现有 agent → 对标所有，优点全要缺点全避

## 3. 目标用户

- **主力**：作者自己（单用户深度使用，花活全开）
- **附带**：开源后未知设备环境的其他用户（次要）

## 4. 核心功能（立项级，不细化实现）

### 4.1 后端（大脑）
单文件二进制纯后端中枢：多 LLM 适配与多 key 轮询、Agent 引擎与 subagent 长程、知识库唯一记忆系统与编译蒸馏、MCP 工具与对外暴露、心跳调度与服务器监控降级、备份自升级、统一事件信封、logme 足迹、时间戳全局化、权限三层级、API 全功能暴露。

### 4.2 前端（眼耳口手）
多环境纯前端（安卓优先+Win+主流桌面）：作为用户与 agent 的纯桥梁、设备控制层（被后端操控）、内嵌浏览器代理（subagent 独立 tab）、多前端互联直连、灵动岛与保活与小组件、聊天内核与 rewind、知识库前端、备份与设置、SSH/SFTP 直连、自动更新、Plan/Build 双模式。

### 4.3 互联
前后端强分离；多环境适配（内网直连/DDNS-IPv6/IPv4 经中继转 IPv6，方向已定方案待定）。

## 5. 可行性结论

- **技术**：全部需求方向可行，无不可实现项
- **成本**：近零成本（免费 LLM + 免费 CI + 自有硬件）
- **时间**：不预设硬时间表，按节奏每轮确认后推进
- **风险**：中等，无致命项；最大变量是小设备资源与沟通成本，用户态度已明确

## 6. 优先级

> **大脑（后端） > 感官（前端） > 互联花活**

## 7. 作废项

前端 Flutter / 宠物猫 / 对话定位悬浮窗 / 环境变量浮窗 / 任务追踪文件机制——均作废或移除。

## 8. 待定项（立项阶段不拍板，留给后续相应阶段）

MacroDroid 落地细节 / 灵动岛五厂商优先级 / 授权超时 / PC 驱动级控制 / 互联方案具体 / 后端配合前端端点优先级 / 其他未定细节。

## 9. 红线（立项级，方向性，不卡参数）

1. 部署形态：单文件二进制，下载即运行，最多释放必要配置/db/运行时文件夹
2. 前后端关系：前端=用户与 agent 的纯桥梁；一切动手操作归后端；特殊直连通道不经过 agent
3. 记忆系统：知识库=唯一记忆系统；logme 走文件不进主库
4. 端口：默认 8086，可配
5. 优先级：大脑 > 感官 > 互联
6. 开源：为用 GitHub Actions 免本地部署安卓环境，代码上传是附带结果
7. 中文优先：非机器语言一律简体中文
8. 进度节奏：严格遵循进度跟踪文档，每步确认后推进，不跳步

## 10. 第 1 步完成标志

本 PRD 经作者确认并写入进度跟踪文档后，**第 1 步：立项与规划——完成**。项目正式"立起来"。下一阶段为第 2 步：设计阶段。
