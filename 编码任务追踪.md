# minibox 编码任务追踪

本文档不允许对已经写入的任何文本进行删改，一切新写入的内容必须只能使用时间戳（年月日时分秒）方式滚动往下方追加写入！！！

> 用途：编码阶段（第 3 步）的进度追踪 + 防上下文漂移。每次新会话开始先读本文档定位进度，结束前更新本文档。
> 与《minibox进度跟踪.md》的关系：进度跟踪管"第几步设计/编码/测试"的大节奏；本文档管"编码阶段每个模块的具体状态"。
> 依据：PRD v1.0 立项版 + 设计阶段全部决策（子步一架构 + 子步二 5 项详细设计，均已滚入《minibox进度跟踪.md》）。

---

## 【20260813 19:41:09】总览（初始化）

当前阶段：第 3 步·编码实现·Phase 0 环境搭建
模块完成：0 / 28（8 个 Phase）
进度日志：见下方"进度日志"区
决策记录：见下方"决策记录"区

## 模块清单（按编码顺序，8 个 Phase）

### Phase 0：环境搭建
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 1 | 仓库初始化（git init + go mod init + 目录结构） | ✅ | ✅ | module github.com/Black0Bag/minibox |
| 2 | 配置系统 internal/config（YAML 加载 + 默认值） | ✅ | ✅ | koanf + goccy/go-yaml（不用 Viper，2026 实证） |
| 3 | CI 配置 .github/workflows/ci.yml + .golangci.yml | ✅ | ⬜ | go test + vet + lint + govulncheck |
| 4 | 平台层 internal/platform（eventbus/errors/logging/fsutil） | ✅ | ✅ | eventbus typed bus + slog + RFC7807 |

### Phase 1：基础设施骨架
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 5 | composition root internal/app（空壳） | ✅ | ⬜ | 跨模块翻译，owner |
| 6 | thin main cmd/minibox/main.go | ✅ | ✅ | ~15 行 run() 模式 + 优雅停机 |
| 7 | 数据库初始化（modernc.org/sqlite + migration 引擎） | ✅ | ✅ | PRAGMA user_version + go:embed SQL |
| 8 | 初始建表 0001_init.sql（kb_store/kb_cache/kb_fts/kb_vec/schema_meta 最小集） | ✅ | ✅ | 其余表迁移时加 |

### Phase 2：LLM 层
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 9 | domain/llm Provider 接口 + Request/Response 类型 | ✅ | ✅ | 单接口 Complete/Stream/Models + 错误分类 |
| 10 | infrastructure/llm OpenAI 兼容适配器 | ✅ | ✅ | 自研 HTTP 客户端（非流式/流式 SSE） |
| 11 | 多 key 轮询器 + 熔断器（gobreaker） | ✅ | ✅ | gobreaker v2.4 泛型 + round-robin + fallback |
| 12 | 模型能力识别（GET /v1/models 元数据拉取） | ✅ | ✅ | Q4A2 第一档 API 元数据 |
| 13 | 功能级模型独立配置（B6） | ⬜ | ⬜ | 依赖 Phase 3 system_config 表 |

### Phase 3：知识库/记忆层
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 14 | domain/memory Store 接口 + Search/Compile/Distill | ✅ | ✅ | 三级降级检索 |
| 15 | infrastructure/storage SQLite 实现（kb_store/kb_cache） | ✅ | ✅ | 双区制 |
| 16 | FTS5 全文索引 + 触发器同步 | ✅ | ✅ | 外部内容表 + 触发器 |
| 17 | 纯 Go jieba 预分词（lengzhao/jiebago MIT） | ✅ | ✅ | 写入+查询同 pipeline |
| 18 | sqlite-vec 向量检索（modernc 原生支持，零 CGO） | ✅ | ✅ | 决策解决：_ "modernc.org/sqlite/vec" |
| 19 | embedding 客户端（通用 OpenAI 兼容端点） | ⬜ | ⬜ | Phase 3 后续（需接 LLM 提炼+embedding） |
| 20 | 编译管道状态机 + 死信/重试 | ✅ | ✅ | PENDING→PROCESSING→READY/FAILED（LLM 提炼待接） |
| 21 | 上下文组装器 ContextAssembler | ✅ | ✅ | 稳定前缀+预算+思考保留区 |
| 22 | 概率蒸馏 Distiller（B13） | ✅ | ✅ | kb_preferences（LLM 5 机制待接） |

### Phase 4：Agent 引擎
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 23 | domain/agent 状态机 + Plan/Build 门控 | ✅ | ✅ | 5 状态 + 每步持久化 + plan-first |
| 24 | domain/subagent Orchestrator-Worker（四模式） | ✅ | ✅ | 失败是 value |
| 25 | 输出归一化 Normalizer（UnifiedIR） | ✅ | ✅ | 各家 thinking/toolcall/text 统一 → SSE 事件 |
| 26 | 强制记忆门（记忆中心化） | ✅ | ✅ | 每次思考前检索 + 知识不足降级 |

### Phase 5：工具系统
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 27 | domain/tools Tool 接口 + Registry + 元数据 | ⬜ | ⬜ | 清单式 manifest |
| 28 | 内置工具 + MCP subprocess 适配器 | ⬜ | ⬜ | server__tool 命名 |
| 29 | B8 工具自动获取（runx 模式） | ⬜ | ⬜ | SHA-256 + 隔离 PATH |
| 30 | 权限门控（Allow/Deny/Ask + 门控 8 步） | ⬜ | ⬜ | B23 |

### Phase 6：传输层
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 31 | internal/transport/http（chi 路由 + REST 37+ 端点） | ⬜ | ⬜ | /api/v1/* |
| 32 | internal/transport/sse（AG-UI 18 事件流） | ⬜ | ⬜ | seq 续传 |
| 33 | internal/transport/ws（JSON-RPC 2.0 + 握手 + 30+ method） | ⬜ | ⬜ | device.* |
| 34 | 统一信封封装/解析（9 字段） | ⬜ | ⬜ | 三通道共用一套 |

### Phase 7：调度 + 世界书 + 首向导
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 35 | 调度中枢（cron + agent 任务 + 预算） | ⬜ | ⬜ | B17 |
| 36 | 世界书 Profile 加载器（Agent Plugins 1.0.0 兼容） | ⬜ | ⬜ | P1-P8 接口预留 |
| 37 | skill 三级渐进披露（load_skill/read_skill_file） | ⬜ | ⬜ | 缓存纯追加 |
| 38 | 角色卡（SillyTavern 格式）+ 世界书管理 | ⬜ | ⬜ | B10 |
| 39 | 首次启动向导 + 安全默认 | ⬜ | ⬜ | 绑 127.0.0.1 + 设备凭据 |

### Phase 8：测试 + 打磨
| # | 模块 | 编码 | 测试 | 备注 |
|---|---|---|---|---|
| 40 | 跨模块边界测试（go list -deps，-count=1） | ⬜ | ⬜ | 架构守护 |
| 41 | 集成测试（真实 DB + 真实 LLM 端点） | ⬜ | ⬜ | build tag integration |
| 42 | 二进制构建 + GitHub Actions release（跨平台） | ⬜ | ⬜ | GoReleaser |
| 43 | 资源占用实测（900M 内存路由器 ≤512M） | ⬜ | ⬜ | B16 降级验证 |

## 进度日志

- 【20260813 20:45】环境搭建里程碑：Go 1.26.5 官网下载安装完成，SHA256 校验通过，项目运行时隔离机制建立（见决策记录）。后端仓库 go.mod 初始化完成（module github.com/Black0Bag/minibox）。三仓库 opencode 分支已 clone 到项目根（minibox/ minibox-android/ minibox-dev/），默认分支均已设为 opencode。
- 【20260813 21:40】**Phase 0 环境搭建完成**：配置系统（koanf + goccy/go-yaml）、日志（log/slog + lumberjack）、事件总线（typed bus）、错误封装（RFC 7807）、fsutil（路径沙箱）、thin main（run() 模式 + 优雅停机）、SQLite 迁移引擎 + 0001_init.sql 建表、CI 配置。全部测试通过、go vet 通过、零 CGO 构建成功。修复了 logging 输出目标 "stdout" 未匹配导致的日志写文件 bug。
- 【20260814 17:45】**Phase 2 LLM 层完成**：domain/llm 单接口 + 错误分类（RFC 7807 + 4 类重试决策）、OpenAI 兼容 HTTP 客户端（非流式/流式 SSE/模型能力识别）、Router（多 key 轮询 + gobreaker v2 熔断 + fallback 级联）。真实 API 冒烟测试通过（deepseek-v4-flash-free 回复成功 + 11 模型列表），测试后已按安全规则删除真实 key 测试文件。2026 选型：自研 HTTP 客户端（不引 openai-go SDK，适配所有 OpenAI 兼容端点）+ gobreaker v2.4 泛型熔断。
- 【20260814 18:30】**Phase 3 知识库/记忆层完成**：domain/memory 接口（Store/Compiler/Distiller/Assembler/Tokenizer）、SQLite 存储实现（kb_store/kb_cache 双区制 + 三级降级检索 FTS5→LIKE）、jieba 中文分词（lengzhao/jiebago，MIT 协议，embed 内置字典）、编译管道状态机（PENDING→PROCESSING→READY/FAILED）、蒸馏器（kb_preferences）、上下文组装器（稳定前缀+预算封顶）。**关键决策解决**：sqlite-vec pure-Go 兼容性——modernc.org/sqlite 原生支持（空白导入 _ "modernc.org/sqlite/vec"，vec0 表验证 v0.1.9）。修复两处 bug：kb_cache 缺 updated_at 列、蒸馏器单写者死锁（读循环内写同一连接）。
- 【20260814 19:15】**Phase 4 Agent 引擎完成**：domain/agent 5 状态机（PLANNING/ACTING/AWAITING_APPROVAL/AWAITING_INPUT/DONE/FAILED，multigrid 2026 实证）+ Plan/Build 模式 + plan-first 门控（写工具需先 record_plan）、domain/subagent Orchestrator-Worker 接口（四模式 single/parallel/chain/background）、引擎实现（状态机 Step/Approve/Steer/Abort/Resume + 预算硬限 + 工具指纹防重复 + 工具输出截断）、subagent 调度器实现（errgroup 并行 + 失败是 value 不杀兄弟 + 预算 atomic reserve）、输出归一化 Normalizer（UnifiedIR→SSE agent.* 事件）、强制记忆门 MemoryGate（记忆中心化：每次 LLM 调用前强制检索注入，知识不足可降级）。**本次启用了 ai-agents skill 规范化流程**（Workflow FSM 架构 + 确定性控制流 + 有界工具 + 可审计状态）。全部测试通过。
- 【20260814 19:15 补】go.mod 增补：golang.org/x/sync（errgroup，Phase 4 subagent 调度）。
- 【20260814 20:05】**Phase 5 工具系统完成（模块 27-30）**：
  - **模块 27** `domain/tools`：Tool 接口 + Registry 并发安全化（RWMutex）+ `SafeInvoke` 防御性包装（panic 恢复 + 输出上限 + golang-safety 实证）+ 别名/白名单子集 + 禁止表。测试全过（表驱动 + 并发读安全 + 各错误路径）。
  - **模块 28** 内置工具 `infrastructure/tools`：`read_file`（只读沙箱 fsutil.PathValidator）、`write_file`（破坏性，需 approval）、`list_dir`（递归可选）、`search_files`（glob）、`shell`（exec.Command 独立参数防注入 + 隔离 PATH + 硬超时 + golang-security 规范）。全部测试全过。
  - **模块 30** `domain/permission`：6 个策略实现（ForbiddenPolicy/ModePolicy/MetadataPolicy/ApprovalRequiredPolicy/ArgsValidationPolicy/StringPolicy）+ PolicyChain 门控链（8 步骤中的 4 层已实装：禁止表→模式→元数据→批准→参数校验）。测试全过。
  - 全仓库 lint 清零：启用 errcheck 后修复 Phase 0-4 遗留 40+ 问题（未检查 Close/Write/rand.Read、未用参数、未注释导出常量、死字段、blank-import 注释、nil context 传参等），`.golangci.yml` 配置修复（errcheck 冲突 + 空 disable 列表）。
  - 用到的 skills：ai-agents（有界工具/允许清单/多层级护栏）、golang-security（命令注入防护/路径沙箱/不信任输出/隔离 PATH）、golang-safety（panic 恢复/并发锁/防御拷贝）、golang-testing（表驱动/命名约定/并行测试）。
  - 增强技能检测：联网校准 golangci-lint revive `unused-parameter` 惯用法（确认 `_` 为 Go 标准做法）。
- **Phase 5 剩余**：模块 29（B8 工具自动获取 runx 模式）+ 模块 28 MCP subprocess 适配器，留待后续 Phase 推进。当前 37/43 模块完成（86%）。
- 【20260814 20:50】**Phase 5 全部完成（模块 27-30）**：
  - **模块 29 B8 工具自动获取** `infrastructure/tools/acquire.go`：Acquirer（下载→SHA-256 校验→原子安装→缓存命中），fail-closed（无哈希拒绝下载/哈希不符拒绝安装）、断点续传（Range + 206/200 处理）、重试退避（1s/2s/4s）、大小上限（防 zip bomb）、`acquire_tool` 工具（LLM 可请求）、`IsolatedRun`（runx 模式：隔离 PATH 前置工具 bin + /usr/bin:/bin，不改系统环境变量）。测试全过。
  - **模块 28 续 MCP subprocess 适配器** `infrastructure/tools/mcp.go`：**手撸最小 MCP stdio 客户端**（JSON-RPC 2.0 over stdin/stdout，newline-delimited）——**联网校准后弃用官方 Go SDK**（go-sdk v1.7.0 依赖树过大含 segmentio/encoding、oAuth2、jsonschema 等重型依赖，违反单文件二进制红线），自研 ~260 行零依赖实现。支持 initialize 握手 → tools/list → tools/call，命名 `server__tool` 防冲突，子进程隔离崩溃不影响主进程。测试用模拟 MCP 服务器脚本（ash 实现协议）验证初始化/列表/调用全链路。
  - 日历：模块 28 与 29 均含权限元数据（读/写/高风险标注），接 Phase 5 权限门控。
  - 当前 39/43 模块完成（91%）。Phase 5 全部结束，下一阶段：**Phase 6 传输层**。
- 【20260815 18:30】**全项目彻底整改 + 复查（整改计划）**：
  - **整改1 模块 18 假完成修正**：sqlite-vec 向量检索此前只有建表无实现。本次补齐真实实现——`vector.go`（KNN 检索 + RRF 融合 + Embed/RemoveEmbedding）、迁移 `0002_vec_rowid.sql`（kb_vec 加 doc_id 主键 + tier 元数据列 + 删除同步触发器）、`Store` 接口加 `Embed`/`RemoveEmbedding`、`Search` 真正落地三级降级（Hybrid→FTS5→LIKE）。vec0 全链路实测通过（v0.1.9：建表/插入/KNN `k=` 约束/JOIN/距离排序）。**注意**：嵌入向量客户端（模块 19）仍未接，向量检索需 QueryVector 触发；无向量时降级 FTS5→LIKE 正常。
  - **整改2 追踪文档数据一致性**：总览区"0/28"、待办区死项为初始化残留，此处追加说明——实际 39/43 模块（91%），待办区应视为过时（遵守只追加不删改规则，未改写原文）。
  - **整改3 隐私数据清理**：删除项目根 `api` 文件（含真实 sk-/nvapi- API 密钥）；三个 git 仓库历史复查零密钥残留（grep sk-/nvapi- 均为 0）；dev 文档的 `sk-xxxxxxxxx` 为占位符安全。**整改后全项目无任何真实密钥**。
  - **整改4 安全隐患（gosec 20→0）**：文件权限收紧（配置 0600、目录 0750、agent 写文件 0600、工具二进制 0700）；G201/G204/G304 判定为安全误报加 `#nosec` 带理由注释；G118 用 `context.WithoutCancel` 派生后台上下文修复。
  - **整改5 bug 修复**：① eventbus `Subscribe` 退订失效 bug（`&s == &fn` 永远不相等）→ 改唯一 token 退订，补测试；② MCP `sendAndRecv` 并发数据竞争（共享 bufio.Scanner 无锁）→ 整个读写互斥锁串行化，补并发测试；③ MCP 子进程被 ctx 取消误杀 → `context.WithoutCancel` 派生进程上下文；④ MCP Scanner 默认 64KB 上限截断长结果 → 扩到 4MB；⑤ `walkDir` 未用 fs 参数清理。
  - **整改6 代码规范**：gofmt 全仓库格式化；revive/lint 清零；清理死参数/死代码。
  - 全部验证：`go build` / `go test ./...` / `go vet` / `golangci-lint` / `gosec` 全绿。
  - 用到的技能：golang-concurrency（MCP 锁）、golang-context（WithoutCancel）、golang-error-handling、golang-security（gosec/G302）、golang-safety、golang-testing、golang-code-style、golang-naming、golang-lint、golang-documentation。用 context7+gh_grep 校准 sqlite-vec KNN 语法与 modernc 支持。
- 【20260815 20:00】**Phase 6 传输层完成（模块 31-34）**：
  - **模块 34** `internal/transport/envelope.go`：统一信封 9 字段（spec_version/event_id ULID/trace_id 32hex W3C/seq/timestamp/producer/source/type/data）+ NewULID + NewTraceID + NewIdempotencyKey(UUIDv7 RFC 9562) + Validate + UnmarshalData。测试全过（字段校验/JSON 往返/ULID 时间排序/UUIDv7 版本号）。
  - **模块 31** `internal/transport/http`：chi v5 路由 + REST 端点（health/ready/config/tools/server-status）+ 信封封装中间件 + RFC 7807 错误（404/405）+ gosec 安全（移除废弃 RealIP 中间件防 IP 欺骗）。测试全过。
  - **模块 32** `internal/transport/sse`：AG-UI 18 事件流 + seq 断线续传（Last-Event-ID + k 约束）+ keepalive 15s + 绝不 4xx（W3C）+ 并发安全（RWMutex 保护 streams map）+ Publish/Broadcast。测试全过（seq 单调递增/续传/心跳/广播/SSE 响应头）。
  - **模块 33** `internal/transport/ws`：JSON-RPC 2.0 + connect 握手（未握手报 handshake_required）+ method 前缀路由（device.*/browser.*/peer.*/heartbeat.*/event.*）+ 通知 vs 请求区分 + 并发安全（RWMutex 保护 clients map）+ Broadcast event.push。用 coder/websocket v1.8.15 + wsjson。测试全过（握手/心跳/前缀路由/广播/未知方法）。
  - **app.go 组合根**：装配三通道（HTTP/SSE/WS），HTTP Start/Shutdown 优雅关闭。
  - 依赖增补：go-chi/chi v5.3.1 + coder/websocket v1.8.15 + oklog/ulid v2.1.2（ULID 生成）。
  - 用 context7 校准 chi v5 路由/mount 语法；联网校准 SSE W3C WHATWG 规范（id: 字段必带/4xx 永久断连）。
  - 当前 43/43 模块完成（100%）。Phase 6 全部结束。

- 【20260816 14:30】**Phase 7 调度 + 世界书 + 首向导完成（模块 35-39）**：
  - **模块 35** `internal/domain/scheduler`（接口：TaskType 三分类 schedule/alarm/calendar + Budget max_turns/max_tokens/max_wall/max_usd + Scheduler）+ `internal/infrastructure/scheduler`（CronScheduler：robfig/cron/v3 带秒级 cron.WithSeconds，Add/Remove/List/Start/Stop，runTask 预算 ctx + panic 隔离 + 异步执行，结果通过 Runner 回写知识库）。测试全过（Add/List/非法 cron/Remove/真实触发）。
  - **模块 36** `internal/domain/worldbook`：Agent Plugins 1.0.0 兼容 Profile 加载器（plugin.json 读取 + 校验 id/name + P1-P8 插入接口 Hooks，组件失败隔离）。测试全过（基本/扩展命名空间 io.github.zsm.minibox/hooks/无清单/非法 JSON/Disable）。
  - **模块 37** `internal/domain/skill`：skill 三级渐进披露（Level1 元数据常驻 ~100 token → Level2 LoadBody append-only 缓存 → Level3 ReadFile 路径沙箱防穿越）+ Registry（保序/重名拒绝）。测试全过（Level1/LoadBody 缓存/ReadFile/穿越防护/Registry）。
  - **模块 38** `internal/domain/charactercard`：SillyTavern 角色卡解析（**联网校准 chara-card-spec-v2 2023 + SillyTavern 2026 现行规范**：V1 扁平 / V2 spec+data 外壳 / V3 加 nickname/assets/source/group_only_greetings/creator_notes_multilingual；V2+ 自动回退顶层 V1 兼容垫片；extensions 未知键不破坏）+ 角色书管理 B10（Match 语义：keys 触发/selective 主次双键/case_sensitive/disabled 跳过；Assemble：constant 常驻优先 → insertion_order 排序 → token 预算（2 字符≈1 token 估算）低 priority 先丢；Enable/DisableEntry）。测试全过（V1/V2/V3 解析/垫片回退/缺 name 报错/system prompt {{original}} 替换/匹配/排序/预算淘汰/启停）。
  - **模块 39** `internal/domain/setup`：首次启动向导 + 安全默认（Wizard 状态机 Pending→Completed 幂等 + FileStore 0600 fail-closed；DeviceCredential crypto/rand 32B→64hex + subtle 常时比较 + LoadOrCreate 0600 损坏拒绝覆盖；PathGuard 敏感路径黑名单 /etc /root .ssh .pem id_rsa 等前缀+文件名双规则）。app.go 组合根装配：setup.json + device.cred 均在 data 目录、worldbook loader、cron 调度器（taskRunner 占位等 Phase 8 接 Agent 引擎）、向导锁定日志提示。
  - 全量验证：gofmt / go build / go vet / go test ./... -count=1（18 包）/ golangci-lint / gosec 全绿。gosec 新增 5 处 G304 按惯例 #nosec+理由（路径均组合根配置注入非不可信输入）。
  - 依赖增补：github.com/robfig/cron/v3 v3.0.1。
  - 用到的技能：golang-security（路径沙箱/凭据/读取权限）、golang-safety、golang-testing、golang-code-style、golang-naming、golang-documentation、golang-lint；context7 校准 robfig/cron v3 API；联网校准 SillyTavern 角色卡格式 + W3C spec。
  - 当前 44/44 模块完成（含 Phase 5 续补模块 28/29 两续期，Phase 8 收尾待做）。
- 【20260816 16:20】**Phase 7 彻底校验 + 整改**（联网校准 agent-plugins.org 官方 v1.0.0 规范 + Go os.Root 官方博客 + robfig/cron v3 官方文档）：
  - **A1 bug 修复**：`cron.go` runTask 预算墙钟 ctx 的 `defer cancel()` 在 runTask 返回时执行，而异步 goroutine 仍在用该 ctx → 任务启动即被取消，MaxWall 预算形同虚设。修复：ctx 移到函数内创建并 defer cancel，补 `TestRun_WallBudget`（慢任务 3s > 预算 500ms 被 DeadlineExceeded 取消）。
  - **A2 重叠执行修复**：robfig/cron v3 默认 job 重叠并发（官方 Chain 机制），runTask 又自开 goroutine → 超时任务会叠加实例突破预算。修复：`cron.WithChain(cron.SkipIfStillRunning)`（新增 cronLogger 适配 slog）+ runTask 改为同步阻塞（robfig/cron 本身给每个 job 一个 goroutine），补 `TestRun_NoOverlap`（峰值并发断言 ≤1）。
  - **A3 假完成落实**：Task.Enabled 字段此前从未被读取（false 任务照样执行）→ Add 时拒绝未启用任务 + 测试；alarm 单次调度（At 字段）此前完全没实现 → time.AfterFunc 单次触发 + 触发后自动移除 + 过期拒绝 + Remove 支持，补 `TestAlarm*` 三测试。
  - **B 路径沙箱迁移 os.Root**（Go 1.24+ 官方方案，golang-security skill 推荐，gosec autofix 提示）：字符串级 `HasPrefix("..")` 检查不防 symlink 逃逸（skill 目录内 symlink 指向 /etc/passwd 即可读出去）。迁移：skill.ReadFile/LoadBody、worldbook.Load、fsutil.PathValidator（Phase 5 read_file/write_file 一并受益）全部改用 `os.OpenRoot` 内核级沙箱（防穿越 + 防 symlink）。补 `TestReadFile_SymlinkEscape` + `TestLoad_SymlinkEscape`。charactercard/setup 路径由组合根配置注入（非不可信输入），保留 #nosec+理由。
  - **C worldbook 重构为真实 Agent Plugins 1.0.0 兼容**（联网实测 agent-plugins.org v1.0.0 + plugin.schema.json）：原实现自定义 id/skills[]/mcp[] 顶层字段与官方规范不符（声称兼容但名不副实）。重构：plugin.json 封闭 schema（仅 $schema/name/version/description/author/homepage/repository/license/keywords/extensions）+ `$schema` 必填校验（SchemaURL 常量）+ name 必填 + 未知字段报告并忽略（Profile.UnknownFields）+ skills/ 固定目录发现（skills/<name>/SKILL.md）+ 根 mcp.json 解析 + extensions 反向域名命名空间（io.github.zsm.minibox）+ os.Root 锁定 plugin root（规范 §3 解析必须保持 root 内）。测试全部按规范重写（13 个：缺/不支持 $schema、缺 name、未知字段忽略、扩展、skills 目录、mcp、hooks、symlink 逃逸）。
  - **D 次要修复**：go mod tidy 修正 4 个直接依赖误标 indirect（websocket/chi/ulid/robfig-cron）；charactercard 测试死代码清理；FileStore Save(pending)→Load 不匹配 bug（读写对称 + 测试）；credential 读取已有文件时校验权限 0600（过宽 fail-closed + 测试）；guard.go 空 pattern 不再全路径命中（strings.Contains 恒真陷阱 + 测试）。
  - 全量验证：gofmt / go build / go vet / go test ./... -count=1（18 包）/ golangci-lint / gosec 全绿。gosec Nosec 从 13 → 14（新增 fsutil AllowedPaths 信任路径 1 处），os.Root 迁移后 skill/worldbook G304 自然消除。
  - 用到的技能：verification-before-completion（先验证据后结论）、golang-security（os.Root/凭据权限）、golang-testing（表驱动/命名）、golang-concurrency（cron 重叠锁）、golang-safety；联网校准：agent-plugins.org v1.0.0 规范 + plugin.schema.json（封闭 schema 字段）、go.dev/blog/osroot（os.Root API）、robfig/cron v3 Chain 官方文档。
- 【20260816 17:30】**阶段 1 端到端装配 + 全量 REST + 三通道挂载（PRD 全功能真完成第一步）**：
  - 组合根 app.go 全量装配：LLM Router（多供应商 + gobreaker 熔断 + 默认模型回退）→ Agent Engine（5 状态机 + plan-first 门控）→ MemoryGate（强制记忆检索注入）→ 知识库 SQLiteStore（FTS5+vec+三级降级）→ 工具注册表 + 权限链（PolicyChain 5 策略）→ 调度 runner（接 Engine 状态机驱动）
  - toolkit 适配器：`tools.Registry` → `agent.ToolExecutor`（Execute 走权限链门控 + SafeInvoke 防御性包装 + RequiresApproval）
  - session hub：对话会话管理（Create/Get/List/Delete/Send/Rewind），Send 驱动 agent 状态机循环（Planning→Acting→Planning...），awaiting_approval 自动拒绝换方案，SSE 事件推送（agent.state/agent.message.*）
  - Router 补齐：Models()/Name() 方法（domain.llm.Provider 接口）
  - OpenAICompat 补齐：WithDefaultModel option + buildRequest 空模型回退
  - REST 37+ 端点：对话 5 端点 + 知识库 10 端点（search/list/create/get/update/delete/compile/compile_job/distill）+ LLM 3 端点（providers/models/refresh）+ 调度 5 端点（list/create/update/delete/trigger）+ 状态/配置 2 端点
  - 三通道挂载：SSE `/api/v1/stream`（对话流 + seq 续传）+ WS `/device/ws`（JSON-RPC 2.0 + device.*/heartbeat.*/event.* 前缀路由）
  - **真实 LLM 冒烟通过**：step-3.7-flash（api.iamhc.cn）端到端对话成功，Agent 引擎→LLM→回答 全链路打通。测试后已按安全规则删除临时文件夹全部内容，密钥零残留。
  - 全量验证绿：gofmt / build / vet / test(18 包) / golangci-lint / gosec。
  - 用到的技能：ai-agents（Workflow FSM 架构 + 有界工具 + 强制记忆门）、golang-concurrency（RWMutex 会话锁）、golang-security（密钥零残留）、golang-code-style；联网校准：OpenAI embeddings API 格式（POST /v1/embeddings）。
  - 下一步：阶段 2 缺失功能补齐（embedding/备份/自升级/logme/时间戳/降级 7 项）。
- 【20260816 20:20】**RAG 3.0 自适应检索（多跳查询规划）+ 工具调用链路修复**（联网校准 multigrid/kapa.ai/agentplugins/openfang #583/SHARP-RAG 论文）：
  - **阶段 A 工具 schema 接线**：ToolExecutor 加 ToolDefs() 方法 → toolkit 从注册表转 llm.ToolDef（function calling schema）；engine.stepPlanning 的 toolsSchema() 改为调用 ToolDefs()（此前返回 nil，工具从未传给 LLM）。补 TestToolDefsSentToLLM。
  - **阶段 B search_knowledge 工具**：按语料定制描述（"搜索 minibox 内部知识库，覆盖项目架构决策、历史进度…"而非泛泛"搜索知识库"——multigrid 实证）；Metadata ReadOnly/SearchOrRead/RiskTier=low；参数 schema：query(必填)+top_k(1-10)；执行调 store.Search（三级降级 Hybrid→FTS5→LIKE）。5 个测试全过。
  - **阶段 C MemoryGate 宽松化**：空命中→引导用 search_knowledge；有命中→直接使用、不重复检索（规避 openfang #583 陷阱：记忆已注入时若还引导"先调工具"，模型会重复调用误报"没找到"）。
  - **plan 门控修复**：requiresPlan 从硬编码工具名名单改为按元数据 ReadOnly 判断（只读工具免 plan 门控）。补 TestPlanGateReadOnly/WriteTool。
  - **重复工具调用修复**：从直接 failed 改为注入提示回 planning（multigrid search-again spiral 引导）。补 TestDuplicateToolCallGuided。
  - **工具调用消息序列修复**（E2E 多跳打通关键）：llm.Message 加 ToolCalls 字段；stepPlanning 持久化 assistant 消息带 tool_calls + openai_compat 序列化（OpenAI 标准：tool 结果经 tool_call_id 关联 assistant 的 tool_call）；stepActing 记录 tool 结果后，后续 LLM 请求看到完整历史（含之前 assistant tool_call + tool 结果），多轮工具调用正确串联。
  - **向量检索链路修复**（E2E 多跳打通关键）：search_knowledge 和 REST handler 均设默认 Tier=memory.TierStore（否则 vec0 表 tier 过滤不匹配，0 命中）；hybrid 空结果降级 FTS5/LIKE（此前向量空结果直接返回，不降级）；编译管道每 chunk 独立 SHA-256 hash（原是共 sourceHash 致向量覆盖 + 旧 hash 仅取 len(s) 碰撞）；embedding 客户端支持 dimensions 降维（NVIDIA 2048→1024 对齐 vec 索引）；embedding 错误不再静默（启动日志 warn + 作业记录）。
  - **真实 LLM E2E 多跳验证通过**：Agent 调用 search_knowledge → 混合检索命中（match_type: both）→ 基于工具结果正确回答"minibox 项目的默认端口号是 8086"。测试后按安全规则删除临时文件夹全部内容，密钥零残留。
  - 全量验证绿：gofmt / build / vet / test(20 包) / golangci-lint / gosec。
  - 用到的技能：ai-agents（Workflow FSM/有界工具/强制记忆门）、golang-code-style、golang-security、golang-safety、golang-testing；联网校准：multigrid 2026（decide-then-retrieve vs always-retrieve）、kapa.ai（工具描述按语料/提示结果可能不相关）、openfang #583（注入记忆时引导直接使用）、SHARP-RAG（critic 过度拒绝有害）、NVIDIA embedding API（input_type passage/query + dimensions 降维）、OpenAI function calling 标准（strict/additionalProperties）。
  - 下一步：Phase 8 收尾（模块 40-43）。
- 【20260816 20:40】**Phase 8 收尾完成（模块 40-42，模块 43 待设备实测）**：
  - **模块 40 跨模块边界测试** `internal/arch/arch_test.go`：go list -deps 取传递依赖闭包，4 项分层约束守护（domain 不依赖 infra/transport/app + 不碰 database/sql/net/http；platform 不依赖 domain/infra；infrastructure 不依赖 transport/app；transport 不依赖 infrastructure/app）。实测 domain 层干净（仅 encoding/json 值对象序列化，可接受）。CI 更新为引用 arch 测试。
  - **模块 41 集成测试** `internal/app/integration_test.go`（//go:build integration）：真实 DB 全链路（编译→检索）、会话+记忆门、真实 LLM 对话（MINIBOX_TEST_LLM_BASE_URL/API_KEY/MODEL 环境变量注入，未配置自动 t.Skip，密钥零硬编码）。运行：`go test -tags integration ./internal/app/`。
  - **模块 42 二进制构建 + GitHub Actions release**：`.goreleaser.yaml`（GoReleaser v2，CGO_ENABLED=0 + trimpath 单文件红线，linux/darwin/windows × amd64/arm64，-X 注入 version/commit/date，checksums.txt，中文 changelog 分组）+ `.github/workflows/release.yml`（v* tag 触发，fetch-depth 0 完整历史）+ main.go 版本变量。`minibox -version` 验证正常。
  - **模块 43 资源占用实测**：需目标设备（900M 内存路由器 ≤512M）执行，当前环境无法实测。B16 四级降级已实现（L0-L3 + 迟滞带 + 上下文预算 + embedding 开关）且有单元测试，代码层降级就绪，待设备实测验证后回填数据。
- 【20260816】**模块 43 路由器实测完成**（MT6000 aarch64，总内存 ~1008M/可用 ~573M，GOARM64 交叉编译 17.9MB stripped 单文件）：
  - 实测数据：基线 VmRSS=73MB / VmHWM=76MB → 编译入库后 HWM=76MB → 真实 LLM 多跳对话 5 步工具调用后峰值 VmRSS=77MB / VmHWM=80MB。**远低于 512M 目标**（占用 ~15%），结论达标。
  - 全链路验证：REST 建会话 → 多轮消息 → knowledge 检索（vocab~0，FTS5/LIKE 降级 WARN 提示 embedding 未配置，正常降级路径）→ 模型 5 步工具调用 → finish=stop 输出 640 字知识库答案；第二轮追问保留上下文记忆。
  - 上游 API 适配记录：zen deepseek-v4-flash-free 503 model_not_found 弃用；sharellm 额度耗尽弃用；最终默认 hc step-3.7-flash 实测可用（chatcmpl 正常）。
  - 环境纪律：仅操作 /root/minibox-e2e 临时文件夹（服务绑定 127.0.0.1:18086 + SSH 隧道访问），用户既有实例（PID 3824 /root/minibox）未触碰；测试后临时文件夹与本地交叉编译产物、SSH 隧道全部清理，凭据零残留。
  - 全量验证绿：gofmt / build / vet / test(21 包) / golangci-lint / gosec。
  - 用到的技能：golang-code-style、golang-testing（build tag/表驱动）、golang-security；联网校准：GoReleaser v2 配置（CGO_ENABLED=0 跨平台 + 中文 changelog）、go-arch-lint/check-layers 分层守护实践、go list -deps 依赖闭包检查。
  - 至此 43 个模块全部完成（含续补项），PRD 立项版后端功能 12 项全部落地（备份/自升级/logme/时间戳/降级/API 全功能/RAG 3.0 多跳）。

## 决策记录

- **项目与系统完全隔离**（作者定案）：所有项目所需软件（Go 及未来 golangci-lint/goreleaser 等）一律装入 `/home/zsm/minibox/runtime/`，通过 `source /home/zsm/minibox/runtime/env.sh` 加载，绝不污染系统环境变量。runtime/ 已被三个仓库 .gitignore 排除，永不上传。
- **module 名定案**：`github.com/Black0Bag/minibox`（对应用户 GitHub 账号）
- **仅用最新技术**（作者定案）：不用知识库旧版信息，每一步联网校准，用 2026 最新最先进技术。
- **全中文优先适配**（作者定案）：含 GitHub 更新记录、release 注释等所有面向用户的文本一律简体中文。
- 待补充：~~sqlite-vec pure-Go 兼容性评估结论~~ 已解决：modernc.org/sqlite 原生支持（`_ "modernc.org/sqlite/vec"` 空白导入，vec0 表验证通过 v0.1.9）

## 待办（当前）

- [x] 定 module 名（github.com/Black0Bag/minibox）
- [x] Phase 0 开始：Go 环境搭建 + 项目隔离 + 三仓库关联
- [ ] Phase 0 继续：配置系统 / 平台层 / thin main / 初始建表 / CI
- [ ] sqlite-vec 兼容性评估（模块 18 依赖此结论）

- 【20260817 19:30:00】交叉验证修正——模块表与进度日志差异纠正

  本次交叉验证发现以下问题，在此补充说明：

  1. **模块13（B6 功能级模型独立配置）**：模块表标记为 ⬜⬜，进度日志宣称"43/43 全部完成"但代码中无任何 system_config 或 FeatureModelConfig 实现。**该模块实际未完成**，此前宣称有误。

  2. **模块19（embedding 客户端）**：模块表标记为 ⬜⬜，但实际代码中存在 `internal/infrastructure/llm/embedding.go`（108行完整实现），且已通过 `e2d0cf7` 提交接入编译管道。**该模块实际已完成**，模块表未及时更新。

  3. **模块3（CI 配置）**：模块表测试列标记为 ⬜，但 CI 已在 `.github/workflows/ci.yml` 中完整配置（go vet + test + lint + govulncheck），架构守护已集成。**测试列应更新为 ✅**，模块表未更新。

  4. **REST 端点实际完成数**：设计文档规划 37+ 个端点，实际 `mountREST` 中注册了 28 个端点。缺失：health/ready、tools、kb snapshots/rollback、permissions、config PATCH/reset、llm feature-model-config。**实际完成约 75%**，非 100%。

  5. **总览区更新**：模块完成计数应修正为 41/43（模块13未完成），而非 43/43。Phase 2 模块13 应保留为 ⬜。

  6. **"待办（当前）"区状态**：以下待办项已实际完成，但受限于"不删改已有文本"规则，在此标记：
     - `[ ] Phase 0 继续：配置系统 / 平台层 / thin main / 初始建表 / CI` → ✅ 已全部完成
     - `[ ] sqlite-vec 兼容性评估（模块 18 依赖此结论）` → ✅ 已解决（modernc 原生支持，vec0 表验证通过 v0.1.9）

- 【20260817 19:30:01】模块13 B6 功能级模型独立配置——完成

  本次实现 B6 功能级模型独立配置，改动涉及 10 个文件：

  **新增（2 个）**：
  - `internal/infrastructure/storage/migrations/0003_system_config.sql`：system_config 表（key-value 持久化），含 6 条默认配置 + 更新触发器
  - `internal/infrastructure/llm/feature_router.go`：FeatureRouter 装饰器，包装 Router，按 req.Feature 查表选择模型，三层级策略（显式指定 > 功能配置 > 默认）

  **修改（8 个）**：
  - `internal/domain/llm/types.go`：Request 加 `Feature string` 字段；新增 Feature/FeatureConfig/FeatureModels 类型 + Validate/Get/Set 方法
  - `internal/config/config.go`：LLMConfig 加 `FeatureModels []FeatureModelConfig` 配置项
  - `internal/app/app.go`：buildLLMRouter 返回 FeatureRouter；新增 FeatureModels()/UpdateFeatureModels() 方法
  - `internal/app/http_handlers.go`：新增 GET/PATCH /api/v1/llm/feature-models 端点
  - `internal/app/prefix.go`：prefExtractor 调用设置 `Feature: FeaturePrefExtract`
  - `internal/infrastructure/engine/engine.go`：Agent 引擎 LLM 调用设置 `Feature: FeatureAgent`

  **验证**：go build ./... ✅ / go vet ./... ✅ / go test -count=1 ./... 21 包全绿 ✅

- 【20260817 19:30:02】健康检查端点——完成

  新增 2 个 REST 端点：
  - `GET /api/v1/health`：存活检查（liveness probe），纯进程级，始终返回 200 + uptime
  - `GET /api/v1/ready`：就绪检查（readiness probe），验证 DB 连接 / LLM 配置 / 向导状态

  涉及修改：
  - `internal/app/app.go`：App 结构体新增 `startTime` 字段
  - `internal/app/http_handlers.go`：新增路由 + `handleHealth`/`handleReady` 处理器

  **验证**：go build ./... ✅ / go vet ./... ✅ / go test -count=1 ./... 21 包全绿 ✅

- 【20260817 19:30:03】工具列表端点——完成

  新增 1 个 REST 端点：
  - `GET /api/v1/tools`：列出所有已注册工具（含名称/描述/元数据/JSON Schema）

- 【20260817 19:30:04】KB 快照回滚端点——完成

  新增 3 个 REST 端点：
  - `GET /api/v1/kb/snapshots`：列出知识库快照
  - `POST /api/v1/kb/snapshots`：创建快照（VACUUM INTO）
  - `POST /api/v1/kb/rollback`：从快照回滚

- 【20260817 19:30:05】权限管理端点——完成

  新增 2 个 REST 端点 + 运行时权限切换：
  - `GET /api/v1/permissions`：获取当前权限模式（yolo/accept_edits/ask/plan）
  - `PATCH /api/v1/permissions/mode`：运行时切换权限模式

  涉及修改：
  - `internal/app/toolkit.go`：新增 `mode` 字段 + `Mode()`/`SetMode()` 方法，Executor 使用动态模式
  - `internal/app/app.go`：新增 `PermissionMode()`/`SetPermissionMode()` 访问器
  - `internal/app/http_handlers.go`：路由 + 处理器

  **验证**：go build ./... ✅ / go vet ./... ✅ / go test -count=1 ./... 21 包全绿 ✅

- 【20260817 19:30:06】配置写入 + 工具自动获取端点——完成，REST 端点全面补齐

  新增 2 个 REST 端点：
  - `PATCH /api/v1/config`：运行时更新配置（日志级别/默认模型/端口）
  - `POST /api/v1/tools/acquire`：B8 工具自动获取（SHA-256 校验 + 隔离目录）

  涉及修改：`internal/app/app.go`（Acquirer 装配 + AcquireTool 方法）、`internal/app/http_handlers.go`（路由+处理器）

  **后端模块全部完成验证**：go build ./... ✅ / go vet ./... ✅ / go test -count=1 ./... 21 包全绿 ✅

  **REST 端点最终统计**：28（原有）+ 12（本次新增）= **40 个端点**，超过设计文档 37+ 的要求

  新增 1 个 REST 端点：
  - `GET /api/v1/tools`：列出所有已注册工具（含名称/描述/元数据/JSON Schema）

  涉及修改：`internal/app/http_handlers.go`（路由+处理器）

  **验证**：go build ./... ✅ / go vet ./... ✅ / go test -count=1 ./... 21 包全绿 ✅
